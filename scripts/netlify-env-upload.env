# Netlify env import file — replace placeholders with real secret values before importing into Netlify
# Netlify accepts files with KEY=VALUE lines for bulk import.
# NOTES:
# - KEEP THIS FILE FREE OF SECRETS IF YOU COMMIT IT. Replace values locally or paste into Netlify UI directly.
# - For public values use NEXT_PUBLIC_ prefix. For secret server-only values leave without NEXT_PUBLIC_.

# --- Supabase (required) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Optional aliases
SUPABASE_SERVICE_ROLE=
SUPABASE_SERVICE_KEY=
NEXT_PUBLIC_SUPABASE_CLIENTS_TABLE=AgentHub_DB
SUPABASE_CLIENTS_TABLE=AgentHub_DB

# --- AI provider keys (pick one or more) ---
OPENAI_API_KEY=
OPENROUTER_API_KEY=
GEMINI_API_KEY=
# Optional model / tuning
OPENAI_TEXT_MODEL=
MARKET_OPENROUTER_MODEL=
AI_MODEL=
AI_TEMPERATURE=
MARKET_TEMPERATURE=

# --- Site and routing (public) ---
NEXT_PUBLIC_SITE_URL=https://kairodex.com
NEXT_PUBLIC_BASE_URL=https://kairodex.com
NEXT_PUBLIC_USE_MOCKS=false
NEXT_PUBLIC_USE_SHEET=false
NEXT_PUBLIC_GOOGLE_CLIENT=

# --- Stripe / Payments (if used) ---
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# --- Google / Sheets (if used) ---
GOOGLE_SERVICE_ACCOUNT=
GOOGLE_CLIENT_EMAIL=
# For multiline private keys, either paste the JSON into GOOGLE_SERVICE_ACCOUNT or set GOOGLE_PRIVATE_KEY with newline-escaped value
GOOGLE_PRIVATE_KEY=
GOOGLE_SHEETS_ID=
GOOGLE_SHEETS_GID=

# --- DocuSign (optional) ---
DOCUSIGN_PRIVATE_KEY_BASE64=
DOCUSIGN_CLIENT_ID=
DOCUSIGN_USER_ID=
DOCUSIGN_ACCOUNT_ID=
DOCUSIGN_ENV=demo
DOCUSIGN_REDIRECT_URI=https://kairodex.com/api/docusign/callback

# --- Auth / Sessions ---
JWT_SECRET=
SESSION_IDLE_MIN=20
SESSION_ABS_HOURS=24
HIBP_CHECK=on

# --- CORS / runtime ---
PORT=3000
CORS_ORIGINS=https://kairodex.com
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=60
LOG_LEVEL=info

# --- Misc / feature toggles ---
NEXT_PUBLIC_USE_MOCKS=false
NEXT_PUBLIC_USE_SHEET=false

# Instructions:
# 1. Copy this file locally and fill in secret values (do NOT commit secrets to the repo).
# 2. In Netlify: Site → Site settings → Build & deploy → Environment → "Import variables" and upload this file.
# 3. Alternatively, paste each key/value in the Netlify UI manually. Mark secret keys as protected.
# 4. Deploy site and verify with the tests in scripts/deploy-checklist.md
